name: coordinator_input
description: Coordinate input widget for Flutter with geographic and UTM modes, current location support, and testable MVVM architecture.
version: 1.0.0
homepage: https://github.com/mikaelbotassi/coordinator_input
repository: https://github.com/mikaelbotassi/coordinator_input
issue_tracker: https://github.com/mikaelbotassi/coordinator_input/issues
documentation: https://github.com/mikaelbotassi/coordinator_input#readme
topics:
  - maps
  - coordinates
  - geolocation
  - utm

environment:
  sdk: ^3.8.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
